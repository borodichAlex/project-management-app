import { SortByOrderPipe } from './sort-by-order.pipe';

describe('FilterByOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
